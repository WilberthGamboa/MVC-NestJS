<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>App</title>
  <script src="/bootstrap/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
</head>
<body>
  {{> pcPartial}}
  <div class="container h-100">
    <div class="row justify-content-center align-items-center h-100">
      <div class="col-md-6">
        <div class="card mt-3">
          <div class="card-body">
            <h5 class="card-title">Formulario</h5>
            <form enctype="multipart/form-data" action="" method="post">

              <div class="mb-3">
                <label for="nombre" class="form-label">Nombre</label>
                <input type="text" value={{nombre}} name="nombre" id="nombre" class="form-control" placeholder="Nombre">
                {{#each message}}
                {{#if nombre}}
                <div class="alert alert-danger">{{nombre}}</div>
                {{/if}}
                {{/each}}
              </div>

              <div class="mb-3">
                <label for="descripcion" class="form-label">Descripción</label>
                <input type="text"value={{descripcion}} name="descripcion" id="descripcion" class="form-control" placeholder="Descripción">
                {{#each message}}
                {{#if descripcion}}
                <div class="alert alert-danger">{{descripcion}}</div>
                {{/if}}
                {{/each}}
              </div>

              <div class="mb-3">
                <label for="file" class="form-label">Archivo</label>
                <input type="file" name="file" id="file" class="form-control">
                {{#each message}}
                {{#if file}}
                <div class="alert alert-danger">{{file}}</div>
                {{/if}}
                {{/each}}
              </div>

              <input type="submit" value="Subir al servidor" class="btn btn-primary">
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>